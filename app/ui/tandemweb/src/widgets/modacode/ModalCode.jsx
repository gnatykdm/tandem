import "./ModalCode.css";
import PropTypes from "prop-types";

const ModalCode = ( {userLogin} ) => {
    return (

    );
}

ModalCode.propTypes = {
    userLogin: PropTypes.string.isRequired
}

export default ModalCode;